import { EmptyState } from "@/components/ui/empty-state";

export default function ProjectionsPage() {
  return (
    <EmptyState
      title="Projeções"
      description="As projeções de renda passiva e patrimônio (cenários pessimista / base / otimista) chegam na SPEC-214."
    />
  );
}
