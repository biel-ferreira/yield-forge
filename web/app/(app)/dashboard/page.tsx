import { EmptyState } from "@/components/ui/empty-state";

export default function DashboardPage() {
  return (
    <EmptyState
      title="Painel"
      description="Seu resumo de patrimônio, alocação por setor, Health Score e insights do copiloto chega na SPEC-212 (Dashboard)."
    />
  );
}
