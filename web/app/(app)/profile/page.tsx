import { EmptyState } from "@/components/ui/empty-state";

export default function ProfilePage() {
  return (
    <EmptyState
      title="Perfil"
      description="Seu perfil de risco, objetivos e horizonte de investimento chega na SPEC-210 (Investor Profile)."
    />
  );
}
