import { EmptyState } from "@/components/ui/empty-state";

export default function HealthPage() {
  return (
    <EmptyState
      title="Saúde"
      description="O Health Score 0–100 com o detalhamento por fator chega na SPEC-213."
    />
  );
}
